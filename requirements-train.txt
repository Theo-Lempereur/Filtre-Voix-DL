# Dépendances pour l'entraînement HEADLESS sur GPU NVIDIA (RunPod / serveur distant).
#
# Différence avec requirements.txt : pas de PySide6 (GUI), ipykernel ni
# nbstripout — inutiles sur un pod headless. torch/torchaudio sont tirés de
# l'index CUDA 12.8 (obligatoire pour les GPU Blackwell : RTX 5090).
#
# Installation sur le pod (dans un venv sur le Network Volume) :
#   pip install -r requirements-train.txt
#
# Vérification GPU :
#   python -c "import torch; print(torch.cuda.is_available(), torch.cuda.get_device_name(0))"

--index-url https://download.pytorch.org/whl/cu128
--extra-index-url https://pypi.org/simple

# Deep Learning (CUDA 12.8 — requis RTX 50 / Blackwell)
torch==2.10.0
torchaudio==2.10.0

# Traitement audio
librosa==0.11.0
soundfile==0.13.1
scipy==1.16.3
tqdm==4.67.3

# Données / calcul
numpy==2.0.2

# Visualisation (importée par src/audio.py — librosa.display / matplotlib)
matplotlib==3.10.0

# Suivi d'entraînement
wandb==0.26.1
