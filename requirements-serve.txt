# Dépendances supplémentaires pour servir le modèle via une API HTTP.
# Le reste (torch, librosa, soundfile, numpy) vient de requirements.txt.
#
# Installation :
#   pip install -r requirements.txt
#   pip install -r requirements-serve.txt
fastapi>=0.115.0
uvicorn[standard]>=0.32.0
python-multipart>=0.0.12
python-dotenv>=1.0.0
